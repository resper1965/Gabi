# Development & security dependencies
# Install: pip install -r requirements-dev.txt

# ── Testing ──
pytest>=9.0
pytest-asyncio>=1.0
pytest-cov>=6.0
httpx>=0.27
hypothesis>=6.100

# ── Security ──
bandit>=1.8
pip-audit>=2.9
ruff>=0.8
safety>=3.0

# ── Pre-commit ──
pre-commit>=4.0

# ── Coverage ──
coverage[toml]>=7.0
